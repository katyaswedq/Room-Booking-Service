run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - gosimple
    - gocyclo
    - gofmt
    - goimports
    - revive

linters-settings:
  gocyclo:
    min-complexity: 15

issues:
  exclude-use-default: false